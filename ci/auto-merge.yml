---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cfje/java-buildpack

inputs:
- name: downstream
- name: upstream

outputs:
- name: merged

run:
  path: upstream/ci/auto-merge.sh
